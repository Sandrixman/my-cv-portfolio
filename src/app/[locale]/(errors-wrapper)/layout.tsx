import LayoutWrapper from "@/components/LayoutWrapper"

export default async function ErrorsLayout({ children }: { children: React.ReactNode }) {
    return <LayoutWrapper>{children}</LayoutWrapper>
}
